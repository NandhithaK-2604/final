I need you to fix the EXISTING dashboard UI to closely match the company Pulse dashboard shown in the reference screenshots.

This is a UI/layout task only.

DO NOT change:
- Backend
- APIs
- Business logic
- Existing routes
- Payroll logic
- Transaction logic
- Approval logic
- Validation
- CRUD functionality
- Existing page functionality

Only modify the shared UI components, CSS/theme and layout.

==================================================
1. REMOVE DUPLICATE HEADER
==================================================

My current implementation is displaying the header twice.

There must be ONLY ONE top header.

Find why the Header/Navbar is rendered twice and remove the duplicate.

Final structure:

------------------------------------------------------
☰   Standard Chartered   Straight2Bank          🔔  👤
------------------------------------------------------
|                                                    |
| Sidebar                    Main Content             |
|                                                    |
------------------------------------------------------

==================================================
2. HEADER
==================================================

Create ONE clean dark-navy header similar to the Pulse dashboard.

Header:

- Full width
- Approximately 60–70px height
- Dark navy background
- White/light text

LEFT:

☰  Standard Chartered logo  Straight2Bank

The hamburger must be on the far left.

REMOVE these from the header completely:

- Explore
- Connect
- Resources
- Search icon
- Application/grid icon

RIGHT:

- Notification/bell icon
- Profile/user icon

The notification icon does NOT need complete notification functionality yet.

It only needs to be displayed correctly and styled like the reference.

Make the notification icon a clickable button/component so functionality can be added later.

==================================================
3. PROFILE ICON — IMPORTANT
==================================================

The profile icon must visually match the company Pulse dashboard.

It should NOT look like a random emoji or generic icon.

Use a circular profile/avatar container.

Appearance:

- Circular shape
- Light/white circular background
- Green user/person silhouette inside
- Approximately 38–44px diameter
- Center the person icon perfectly

Example:

       ( white circle )
          🟢
         👤

Use the existing icon library if available.

Prefer a proper user/person icon rather than an emoji.

The profile icon should be placed at the far right of the header.

When clicked, show the existing profile dropdown if one already exists.

The dropdown should contain:

User Name
User Role

Edit Profile

Terms & Conditions
Privacy Notice
Terms of Use

Sign Out

Use existing logged-in user information.

Do NOT hardcode user information if the application already provides it.

==================================================
4. NOTIFICATION ICON
==================================================

Add a bell/notification icon immediately to the LEFT of the profile icon.

Example:

                    🔔   👤

Use a proper icon from the project's existing icon library.

Style:

- White/light icon
- Approximately 20–22px
- Transparent button background
- No large colored square around it
- Proper spacing between bell and profile icon

It does NOT need backend functionality now.

It only needs to be visually present and clickable.

If needed:

onClick={() => {
   // notification functionality can be implemented later
}}

Do not create fake notification data.

==================================================
5. SIDEBAR
==================================================

Keep the existing application sidebar/navigation.

Existing menu items should remain.

For example:

Create
Manage
Approve
Upload
Balances

Do NOT invent new routes.

The sidebar should visually resemble the Pulse dashboard:

- White/light background
- Clean spacing
- Consistent icons
- Dark gray/navy text
- Light borders
- Scrollable when necessary

==================================================
6. SIDEBAR ACTIVE ITEM — VERY IMPORTANT
==================================================

Whenever I click a sidebar item, that item must become BLUE.

For example:

Create
Manage
Approve
Upload
Balances

If I click:

Manage

then Manage should have a blue active state.

If I click:

Approve

then Approve should become blue.

The previously selected item should lose its active state.

The active item should have:

- Blue background OR very light blue background
- Blue/white text depending on the chosen style
- Blue icon
- Clear visual indication that it is selected

Do NOT use the current neon-green active state.

Use the same blue color family as the company Pulse dashboard.

Preferred active styling:

background: #E6F3FF
color: #0072CE

If the Pulse reference uses a stronger blue filled state for the selected item, use:

background: #0A9FE8
color: #FFFFFF

Choose whichever matches the reference more closely.

The important requirement is:

CLICKED SIDEBAR ITEM = BLUE ACTIVE STATE.

==================================================
7. SIDEBAR CLICK BEHAVIOR
==================================================

The sidebar items must continue navigating to their existing routes.

Do not break navigation.

The active state should ideally be based on the current route rather than only a temporary click state.

For example:

/dashboard/createPayroll
    → Create is active

/dashboard/manage
    → Manage is active

/dashboard/approve
    → Approve is active

/dashboard/upload
    → Upload is active

/dashboard/balances
    → Balances is active

Use the application's existing routing system.

Do not hardcode the active item if route information is already available.

==================================================
8. HAMBURGER BUTTON
==================================================

The hamburger/three-line icon must actually work.

When clicked:

IF sidebar is open:
    smoothly slide/collapse sidebar toward the left.

IF sidebar is closed:
    smoothly slide/open sidebar from the left.

Use React state.

Example:

const [sidebarOpen, setSidebarOpen] = useState(true);

Then:

onClick={() => setSidebarOpen(prev => !prev)}

Use a smooth transition:

transition: transform 0.25s ease;

or equivalent existing styling.

Do NOT reload the page.

Do NOT navigate to another page.

==================================================
9. MAIN CONTENT
==================================================

Main page background should match the clean Pulse dashboard style.

Use a very light gray/blue background.

Suggested:

#F5F7FA

Content should use white cards.

Cards:

background: #FFFFFF
border: 1px solid #E5E7EB
border-radius: 8px–12px

Use subtle shadows only.

==================================================
10. FONT — VERY IMPORTANT
==================================================

The font currently does not look close enough to the reference dashboard.

Inspect the existing project and identify:

- Current font-family
- Existing theme
- CSS variables
- Tailwind configuration
- Any imported company font

Use the same font family used by the company Pulse dashboard if it is available to the application.

Do NOT randomly introduce a decorative font.

Apply the font consistently to:

- Header
- Sidebar
- Page titles
- Cards
- Tables
- Buttons
- Forms
- Status badges
- Profile dropdown

Typography should be clean and compact.

Approximate sizes:

Page title: 24–28px
Section title: 18–20px
Normal text: 14–16px
Sidebar text: 14–15px
Header text: 14–15px
Table text: 13–14px
Button text: 14px
Secondary text: 12–14px

Do not make everything bold.

Use medium/semibold only where appropriate.

==================================================
11. COLORS
==================================================

Use the company's Pulse dashboard visual style.

Primary header:

Dark navy

approximately:

#071B3A

Primary blue:

approximately:

#0072CE

Light page background:

#F5F7FA

White:

#FFFFFF

Normal text:

#1F2937

Secondary text:

#6B7280

Borders:

#E5E7EB

Active sidebar:

#E6F3FF
with primary blue text/icon

Avoid neon green.

Green should only be used for appropriate success/status indicators and the profile person icon if matching the reference.

==================================================
12. PROFILE DROPDOWN
==================================================

Make the profile dropdown look like the Pulse reference.

White background.

Subtle border.

Subtle shadow.

Rounded corners.

Position it directly below the profile icon and aligned toward the right edge.

Structure:

--------------------------------
| 👤  User Name               |
|     User Role                |
|                              |
|      Edit Profile            |
|                              |
| Terms & Conditions            |
| Privacy Notice               |
| Terms of Use                 |
|                              |
| Sign Out                     |
--------------------------------

Use the same typography and colors as the rest of the dashboard.

==================================================
13. IMPORTANT — DO NOT CREATE DUPLICATE COMPONENTS
==================================================

Before modifying anything, inspect the project.

Find existing:

Header
Navbar
Sidebar
DashboardLayout
Layout
Profile
ProfileDropdown

If these already exist, modify them.

Do NOT create another Header on top of an existing Header.

Do NOT create another Sidebar on top of an existing Sidebar.

The architecture should preferably be:

DashboardLayout
│
├── Header
│   ├── Hamburger
│   ├── Logo
│   ├── Notification
│   └── Profile
│
├── Sidebar
│   ├── Create
│   ├── Manage
│   ├── Approve
│   ├── Upload
│   └── Balances
│
└── MainContent
    └── Current Page

==================================================
14. PRIORITY
==================================================

I have limited time, so prioritize the changes in this exact order:

1. Remove duplicate header.
2. Make one dark navy header.
3. Put hamburger on left.
4. Remove Explore / Connect / Resources.
5. Add notification bell on right.
6. Make profile icon look like the Pulse reference.
7. Make hamburger open/close sidebar.
8. Make clicked sidebar item BLUE.
9. Fix font family/style.
10. Fix page background.
11. Fix card styling.
12. Verify all existing pages still work.

Do NOT spend time redesigning individual page functionality.

==================================================
15. FINAL RESULT
==================================================

The application should visually feel like the same corporate banking design system as the Pulse dashboard:

- ONE dark navy header
- Hamburger on left
- Standard Chartered/Straight2Bank branding
- Notification bell on right
- Circular green-person profile icon on right
- Working profile dropdown
- White/light sidebar
- Clicked sidebar item becomes BLUE
- Working hamburger sidebar slide animation
- Light gray/blue main background
- White content cards
- Consistent company-style font
- Consistent spacing
- Professional enterprise banking appearance

Most importantly:

DO NOT BREAK EXISTING FUNCTIONALITY.

Implement these changes directly in the existing shared layout/components rather than creating a separate UI.
