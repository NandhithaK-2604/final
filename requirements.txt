Update only the Actions column of the Create Batches table.
Make the action buttons clean, uniform, compact, and professional.
Remove the eye icon from the View button.
View should display only “View” as text.
Edit should display only “Edit” as text.
View and Edit must have exactly the same button style, size, height, border, border-radius, font, padding, and alignment.
Use a blue outlined button with blue text and a clean white/light background.
For Pending, Approved, and Rejected rows → show only View.
For Draft rows → show Edit and Delete.
Keep Delete as a small red outlined button with the trash icon.
Keep all buttons vertically centered and evenly spaced.
Make the Actions column approximately 110–120px wide and ensure it is always completely visible.
Do not introduce a horizontal scrollbar.
Use ellipsis for long Batch IDs instead of allowing columns to expand.
Maintain the existing functionality, click handlers, routing, API calls, and business logic.
Do not modify the sidebar, header, table data, pagination, or other columns.
Target appearance:
Pending     [ View ]
Approved    [ View ]
Rejected    [ View ]
Draft       [ Edit ] [ 🗑 ]
The final design should look like a polished enterprise banking/payment application, with consistent spacing and no unnecessary icons.
