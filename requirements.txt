Current issue: When clicking the ⋮ button, the dropdown overlaps the View button, looks awkward, and can get too close to or outside the table/pagination area. The menu also needs enough width so text such as “View Payee Details” is displayed properly without being clipped.
Make these changes:
Keep the existing View button as the primary action and the ⋮ button as the secondary/more-actions button.
Make the ⋮ button compact and visually clean, around 32–36px, with the icon properly centered.
Make the dropdown menu appear anchored to the three-dot button, rather than using hardcoded positioning that causes overlap.
Give the dropdown a suitable width, approximately 180–200px, so labels like View Payee Details fit completely.
Add a subtle border, shadow, and appropriate border-radius to make the dropdown look like a professional modern menu.
Keep menu items properly aligned with consistent icon spacing and around 40–44px height.
Make the dropdown automatically open upward when there isn't enough space below, especially for rows near the bottom of the table.
Ensure the dropdown stays within the visible viewport/table area and is never clipped.
Make sure the menu does not cover or push the View button in an awkward way.
Preserve the current responsive behavior and make sure it works correctly on different screen sizes.
Do not remove or rename any existing functionality.
Do not modify unrelated components or styling.
After making the changes, check all rows, especially the last row, to ensure the dropdown is positioned correctly.
