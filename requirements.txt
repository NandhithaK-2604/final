Modify ONLY the APPROVE page functionality.

When the user clicks the three-dot (⋮) action menu beside the View button for a batch, provide an option:

👁 View Batch Details

When "View Batch Details" is clicked:

- Reuse the EXISTING "View Payee Details" details UI/component/design already implemented in the application.
- Do NOT create a new details-page design.
- Do NOT create a different modal/drawer style.
- Do NOT change the existing View Payee Details UI.
- Open the same existing details component, but populate it with the selected BATCH's information instead of payee-specific information.

The displayed batch details should correspond to the exact row/batch whose three-dot menu was clicked.

Show the relevant batch information such as:
- Batch ID
- Maker
- Debit Account
- Payees
- Amount
- Payment Date
- Status
- Any other batch-level fields already supported by the existing details component.

IMPORTANT:
The existing View Payee Details functionality must continue working exactly as it currently does.

Reuse the existing component and styling wherever possible rather than duplicating the UI.

Only make the necessary changes to the APPROVE page's three-dot menu and its View Batch Details action.

Do not modify the Manage, Create, Upload, or Balances pages.
Do not modify the overall page layout.
