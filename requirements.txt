Improve only the “Clone Batch” button on the Batch Details page. Do not modify the sidebar, header, table, page layout, or any existing functionality.
Replace the current plain-looking Clone Batch button with a modern secondary action button.
Design requirements:
Position it in the top-right of the Batch Details header, immediately beside the Rejected status badge.
Button text: Clone Batch
Add a clean duplicate/copy icon to the left of the text.
Use a blue outline/border with blue text and icon.
White/transparent background in light mode.
Border radius: approximately 8px.
Height: approximately 40–44px.
Use comfortable horizontal padding, around 16px.
Font should match the existing application's typography.
Add a subtle hover state: light blue background and slightly darker blue border.
Add a subtle transition for hover/focus.
Keep the button visually secondary; the Rejected badge should remain the stronger visual element.
Make sure the icon and text are vertically aligned and have consistent spacing.
Maintain proper responsive behavior so the button doesn't overlap the batch title or status on smaller screens.
Important: Keep the existing Clone Batch click handler/functionality exactly as it is. Only improve its visual styling and placement. Do not create a new component or change business logic unless necessary for styling.
The final result should look like a polished enterprise banking/payment application: clean, minimal, professional, and consistent with the existing SavnyaPay UI.
