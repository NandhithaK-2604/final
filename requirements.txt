Update the existing Approve Batches / Review & Approve page UI and functionality.

IMPORTANT:
Do NOT change, remove, or recreate the existing backend functionality.
The existing Approve, Reject, Cancel, and View Payee Details functionality must continue to work exactly as it does now.

First inspect the existing code and identify the current:
- View button/action
- Review modal/page
- Approve handler/API
- Reject handler/API
- Cancel behavior
- View Payee Details functionality
- Existing validation, confirmation dialogs, loading states, and toast messages

Reuse the existing functions, APIs, components, and business logic wherever possible.

--------------------------------------------------
1. CHANGE "VIEW" BUTTON TO "REVIEW"
--------------------------------------------------

On the Approve Batches page, change the existing "View" action/button to:

"Review"

When the user clicks Review, open the Review & Approve modal.

Do not change the underlying batch selection or data-fetching functionality.

--------------------------------------------------
2. REVIEW & APPROVE MODAL
--------------------------------------------------

When Review is clicked, open a professional centered modal similar to the provided reference design.

Modal title:

"Review & Approve"

At the top-right of the modal, provide:

[ View Payee Details → ]

This should use the EXISTING View Payee Details functionality.

Do NOT put View Payee Details beside the Approve/Reject buttons at the bottom.

--------------------------------------------------
3. BATCH DETAILS
--------------------------------------------------

At the top of the modal, add a section:

"Batch Details"

Display ONLY these fields:

- Batch ID
- Maker
- Batch Date
- Total Transactions
- Amount

Use the existing batch data already available in the application.

Example layout:

Batch Details
────────────────────────────────────

Batch ID              BATCH-0001
Maker                 John Doe
Batch Date            30 Aug 2026
Total Transactions    25
Amount                ₹2,50,000.00 INR

Use a clean two-column label/value layout.

Do not add unnecessary fields.

--------------------------------------------------
4. TRANSACTION SUMMARY
--------------------------------------------------

Below Batch Details, add:

"Transaction Summary"

Display ONLY these fields:

- Account Name
- Account Currency
- Transaction Currency
- Debit Account No
- Payment Date

Example:

Transaction Summary
────────────────────────────────────

Account Name          Operating Account
Account Currency      INR
Transaction Currency  INR
Debit Account No      XXXXXXXX1234
Payment Date          30 Aug 2026

Account Currency should default to INR as required by the existing application/business logic.

Use the actual transaction data when available.

Do not hard-code values except the default Account Currency = INR if that is how the current application is designed.

Do not add extra transaction fields.

--------------------------------------------------
5. MODAL LAYOUT
--------------------------------------------------

Follow the same visual style as the provided reference image.

Structure:

┌───────────────────────────────────────────────┐
│ Review & Approve                View Payee → │
│─────────────────────────────────────────────│
│                                               │
│ Batch Details                                 │
│                                               │
│ Batch ID              BATCH-0001              │
│ Maker                 John Doe                │
│ Batch Date            30 Aug 2026            │
│ Total Transactions    25                      │
│ Amount                ₹2,50,000.00 INR       │
│                                               │
│─────────────────────────────────────────────│
│                                               │
│ Transaction Summary                           │
│                                               │
│ Account Name          Operating Account      │
│ Account Currency      INR                     │
│ Transaction Currency  INR                     │
│ Debit Account No      XXXXXXXX1234            │
│ Payment Date          30 Aug 2026             │
│                                               │
│─────────────────────────────────────────────│
│                                               │
│                    Cancel  Reject  Approve    │
└───────────────────────────────────────────────┘

Keep the modal compact and avoid unnecessary whitespace.

Use:
- Clear section headings
- Proper alignment
- Subtle dividers
- Consistent spacing
- Rounded corners
- Professional enterprise banking/payment UI
- Existing application colors and typography
- Responsive layout
- Scrollable modal body if content exceeds viewport

Do not introduce a completely different design system.

--------------------------------------------------
6. INITIAL BUTTON STATE
--------------------------------------------------

When the Review & Approve modal first opens, show:

[ Cancel ]   [ Reject ]   [ Approve ]

Approve:
- Keep the existing primary blue styling/functionality.

Reject:
- Use a clear destructive/red secondary style.
- Clicking Reject should NOT immediately reject the batch.

Cancel:
- Preserve the existing Cancel behavior.

--------------------------------------------------
7. REJECT FLOW
--------------------------------------------------

When the user clicks Reject:

Do NOT immediately call the rejection API.

Instead, change the modal into a rejection state.

Only then show:

"Rejection Reason *"

[ Select reason ▼ ]

"Comments (optional)"

[ Enter comments... ]

In the rejection state:

- Keep Batch Details visible.
- Keep Transaction Summary visible.
- Keep View Payee Details available.
- Keep Cancel available.
- Hide or disable Approve.
- Show the final red Reject/Confirm Reject action.
- Do not show rejection fields before the user clicks Reject.

Example:

─────────────────────────────────────────────
Rejection Reason *

[ Select reason                         ▼ ]

Comments (optional)

[ Enter comments...                         ]

                         [ Cancel ] [ Reject ]
─────────────────────────────────────────────

The rejection reason must be required.

Use the existing rejection reason options and existing validation if they already exist.

--------------------------------------------------
8. CONFIRM REJECT
--------------------------------------------------

When the user clicks the final Reject button:

- Validate the rejection reason.
- If the reason is missing, use the existing validation behavior or display a clear inline validation message.
- If validation succeeds, use the EXISTING rejection handler/API.
- Do NOT create a new rejection API.
- Do NOT duplicate rejection logic.

If the existing application already has a rejection confirmation dialog, reuse it.

If there is no existing confirmation dialog, show:

"Reject this batch?"

"Are you sure you want to reject this batch? This action cannot be undone."

Buttons:

[ Cancel ] [ Confirm Reject ]

Confirm Reject must trigger the existing rejection functionality.

Preserve the existing success/error handling.

--------------------------------------------------
9. APPROVE FUNCTIONALITY
--------------------------------------------------

When the user clicks Approve:

Keep the existing approval functionality exactly as it currently works.

Do NOT create a new approval API.

Do NOT change the existing business logic.

Use the existing approval handler/function.

Preserve:
- Existing confirmation behavior
- Loading state
- API call
- Success message
- Error message
- Navigation/refresh behavior

Only improve the UI around the existing functionality.

--------------------------------------------------
10. VIEW PAYEE DETAILS
--------------------------------------------------

The "View Payee Details" button at the top-right of the Review & Approve modal must use the existing View Payee Details functionality.

Do not hard-code payee information.

Do not create duplicate payee-detail logic.

Keep the user able to return to the Review & Approve modal after viewing the payee details.

--------------------------------------------------
11. IMPORTANT DATA RULE
--------------------------------------------------

Use the existing data/model/API responses for:

Batch Details:
- Batch ID
- Maker
- Batch Date
- Total Transactions
- Amount

Transaction Summary:
- Account Name
- Account Currency
- Transaction Currency
- Debit Account No
- Payment Date

Do not invent new fields.

Do not add unnecessary information.

Account Currency should default to INR according to the existing requirement.

--------------------------------------------------
12. DO NOT BREAK EXISTING FUNCTIONALITY
--------------------------------------------------

This is primarily a UI/UX improvement.

Do NOT:
- Rewrite backend APIs
- Create duplicate APIs
- Change database structure
- Change existing approval logic
- Change existing rejection logic
- Remove existing validation
- Remove existing confirmation dialogs
- Remove existing success/error handling
- Hard-code batch or transaction information
- Remove View Payee Details functionality

Only modify the UI and component state necessary to create the improved Review → Approve/Reject experience.

--------------------------------------------------
13. FINAL USER FLOW
--------------------------------------------------

The final flow should be:

Approve Batches page
        ↓
Existing "View" action renamed to "Review"
        ↓
Click Review
        ↓
Review & Approve modal opens
        ↓
┌───────────────────────────────────┐
│ Batch Details                     │
│ Transaction Summary               │
│                                   │
│ View Payee Details                │
│                                   │
│ Cancel   Reject   Approve         │
└───────────────────────────────────┘
        ↓
        ├── View Payee Details
        │       ↓
        │   Existing payee details
        │
        ├── Approve
        │       ↓
        │   Existing approve functionality
        │
        ├── Cancel
        │       ↓
        │   Existing cancel behavior
        │
        └── Reject
                ↓
        Show rejection fields
                ↓
        Select rejection reason
                ↓
        Optional comments
                ↓
        Confirm Reject
                ↓
        Existing reject functionality

Make the final UI visually similar to the provided reference image: clean, compact, professional, enterprise banking/payment application style, with Batch Details at the top, Transaction Summary underneath, View Payee Details in the header area, and Approve/Reject/Cancel actions at the bottom.
