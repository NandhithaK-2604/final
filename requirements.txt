Modify ONLY the TABLE VIEW / table component inside the existing "Manage" page.

DO NOT modify the overall Manage page layout.
DO NOT modify the header, navigation, page title, search/filter section, colors, fonts, spacing outside the table, pagination, or any other component.
DO NOT modify other pages such as Create, Approve, Review, etc.

The ONLY goal is to improve the visual design and usability of the existing transaction table.

CURRENT TABLE COLUMNS:
- Batch ID
- Maker
- Debit Account
- Payees
- Amount
- Payment Date
- Status
- Actions

TABLE CHANGES:

1. Keep the exact same columns and data.

2. Improve table spacing and alignment so the rows look clean and professional.

3. RIGHT-ALIGN the Amount values because this is financial data.

4. Keep Status as compact status pills:
   - Pending
   - Approved
   - Draft

5. MOST IMPORTANT — REDESIGN ONLY THE ACTIONS COLUMN.

Currently each row has multiple buttons such as:
Print | View | Edit

This looks crowded and takes too much horizontal space.

Replace it with a compact action design:

For normal/approved/pending rows:
View   ⋮

For Draft rows:
Edit   ⋮

The vertical ellipsis (⋮) opens a small dropdown menu containing secondary actions such as:
- Print
- View Details
- Edit (only if the transaction is editable)

Do NOT use a large filled green Edit button.

The Actions column should be narrow and should not consume unnecessary table width.

6. Keep the existing table header styling and existing blue/white branding.
Only refine the table appearance; do not introduce a new color scheme.

7. Keep sorting indicators if they already exist.

8. Keep the existing pagination exactly as it is.

9. Do not add cards, charts, new filters, new buttons, new sections, or additional information.

FINAL REQUIREMENT:

The result should look like the SAME Manage page as it currently does, but with a more professional and compact TABLE VIEW.

The change should be clearly limited to:
TABLE → row spacing/alignment + Actions column.

Everything outside the table must remain unchanged.
