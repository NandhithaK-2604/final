Use this prompt as-is in your UI/code generation tool:

Improve the Payroll Batch Details page shown in the reference image and make it look polished, modern, professional, and consistent with the existing Pulse application.

Important scope restrictions

- Modify ONLY the Payroll Batch Details page/component.
- Do NOT modify any other page, component, route, service, API, functionality, or business logic.
- Do NOT create or modify global styles.
- Continue using the existing "variables.css" for all colors, spacing, typography, borders, shadows, radii, etc.
- Do NOT hardcode new global colors or CSS variables.
- Reuse the application's existing shared components wherever available, such as Button, Card, Table, Badge/Status, Pagination, Input, etc.
- Do NOT create duplicate versions of existing shared components.
- Do not change the implementation or styling of shared components themselves.
- Do not change global variables or "variables.css".
- Keep all existing functionality, API calls, state management, validation, pagination, add-payee, remove-payee, save-draft, submit-for-approval, and navigation behavior exactly as they currently work.
- This is a UI-only improvement to this page.

Header / navigation

Keep the existing application header and sidebar unchanged. The page should naturally fit into the existing Pulse layout.

Use the existing shared header/sidebar components rather than recreating them.

Page layout

Improve the overall spacing and visual hierarchy.

At the top of the content area:

- Add a subtle "Back to Payroll Batches" navigation link with a back/left arrow.
- Show the batch ID prominently:
  BATCH-1787801993513-553
- Display the current Draft status as a compact status badge.
- Below the batch ID, present the batch information in a clean horizontal metadata section:
  - Debit Account
  - Currency
  - Payment Date
- Use icons only if existing shared icon components are available.

Put this information inside a clean card/container with proper spacing and alignment.

Payee section

Create a clear section titled:

Payee Details (1)

Place the existing:

- "+ Add Payee"
- "Reset Rows"

actions in a logical position without making them visually dominant.

Payee table

Improve the existing table visually while keeping all existing columns and functionality.

Columns should remain:

- Employee ID
- Name
- Account Number
- IFSC/SWIFT
- Amount
- Actions

Use:

- Clean table headers
- Proper column spacing
- Consistent row height
- Subtle borders/dividers
- Good typography hierarchy
- Right alignment for monetary values
- Existing shared table components if available

For the Amount column, display the currency consistently, for example:

₹0.00

Keep the existing Remove functionality exactly as it is.

If a shared destructive button/icon component exists, reuse it instead of creating a new component.

Pagination

Keep the existing pagination functionality.

Improve only its visual alignment and spacing.

Display:

Showing 1 to 1 of 1 payee

along with the existing First, Previous, page number, Next, and Last controls.

Do not change pagination logic.

Summary

Keep the existing summary information:

- Total Payees: 1
- Total Amount: ₹0.00

Present these in a compact, visually clean summary area below the table.

Do not add unnecessary dashboard statistics.

Bottom actions

Keep the existing actions:

- Back to list
- Save as Draft
- Submit for Approval

Make the hierarchy clear:

- Back to list → secondary/outlined
- Save as Draft → secondary action
- Submit for Approval → primary action

Reuse existing shared Button components.

Do not change their functionality, handlers, or API behavior.

Visual design

Aim for a professional enterprise SaaS/Pulse-style interface:

- Spacious but not excessive
- Clean white/light background
- Existing Pulse navy/blue color system
- Subtle card borders/shadows
- Consistent border radius
- Strong typography hierarchy
- Good alignment
- Avoid excessive gradients
- Avoid unnecessary decorative elements
- Avoid adding extra statistics/cards that are not relevant to this page
- Keep the page easy to scan

Responsive behavior

Make the page responsive using the application's existing responsive patterns and shared styles.

On smaller screens, the table and metadata should remain usable without breaking the existing functionality.

Code requirements

Before adding any styling:

1. Inspect the existing Payroll Batch Details component.
2. Identify which shared components are already available.
3. Reuse those components.
4. Check "variables.css" and use the existing variables.
5. Do not introduce duplicate styles or components.
6. Make the smallest possible code changes required for this UI improvement.

Final requirement: Only modify the Payroll Batch Details page and its page-specific styling. Do not modify "variables.css", shared components, global styles, other pages, routes, services, APIs, or business logic.
