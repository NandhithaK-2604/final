There is still a layout issue across ALL User Story 2 dashboard pages.

The main content is positioned too far away from the Sidebar, leaving a large unnecessary empty horizontal gap.

I can see this consistently on:
- Create Payroll
- Manage
- Approve
- Upload
- Balances

IMPORTANT:
Fix this at the SHARED DASHBOARD LAYOUT level instead of adding separate margins to each page.

Requirements:

1. Keep the Sidebar width fixed/consistent.

2. Main content should start reasonably close to the Sidebar with a small, consistent gap.

3. Remove the excessive horizontal whitespace between Sidebar and Main Content.

4. Use a layout such as:

   .dashboard-body {
       display: flex;
       width: 100%;
   }

   .sidebar {
       flex: 0 0 [existing sidebar width];
   }

   .main-content {
       flex: 1;
       min-width: 0;
       padding: 24px 32px;
   }

5. Do NOT use a large margin-left on the page content.

6. Do NOT use fixed positioning to manually place each page.

7. Do NOT use width: 100vw on the main content if it causes overflow.

8. The main content should use the remaining available width after the Sidebar.

9. Keep a consistent small gap/padding between Sidebar and Main Content on every User Story 2 page.

10. The following pages must all have the SAME content starting position:
    - Create Payroll
    - Manage
    - Approve
    - Upload
    - Balances

11. The content cards themselves can have their own internal padding, but their outer position relative to the Sidebar must be consistent.

12. Do not make the content too narrow. It should use the available horizontal space efficiently.

13. Tables should use the available content width. If a table genuinely needs horizontal scrolling, only the table wrapper should scroll.

14. Do not introduce page-level horizontal scrolling.

15. Keep the existing Header and Sidebar design unchanged.

16. Keep the current fonts, colors, icons and active-sidebar styling.

17. Do NOT modify:
    - API
    - routing
    - state
    - validation
    - service layer
    - business logic

18. This is ONLY for User Story 2.
    Do not affect unrelated pages or other user stories.

19. Avoid broad global CSS changes that could affect unrelated pages.

20. Prefer fixing the shared Dashboard/Layout component so all User Story 2 pages inherit the same layout.

VISUAL GOAL:
The Sidebar should end, then there should be a reasonable small gap, and then the main content/card should begin.

Do NOT leave the large blank area currently visible between the Sidebar and the content.

After changing it, verify all five User Story 2 pages at the same browser resolution and make sure their content starts at the same horizontal position.
