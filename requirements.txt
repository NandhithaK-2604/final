Redesign ONLY the Create Payroll / Payroll Batch Details page so that its UI style closely matches the existing Approve page shown in the reference screenshot.

The goal is to make the Create Payroll page feel like it belongs to the exact same application and design system as the Approve page.

IMPORTANT — STRICT SCOPE

- Modify ONLY the Create Payroll page/component.
- Do NOT modify the Approve page.
- Do NOT modify any other page.
- Do NOT modify shared components.
- Do NOT modify "variables.css".
- Do NOT modify global CSS or global styles.
- Do NOT create new global variables.
- Do NOT hardcode a new color system.
- Reuse the existing shared components wherever they already exist.
- Use the existing variables from "variables.css".
- Preserve all existing functionality and business logic.
- Do NOT change API calls, state management, validation, handlers, routing, pagination logic, add-payee logic, remove-payee logic, save-draft logic, or submit-for-approval logic.
- This is a UI/layout/styling improvement only.

DESIGN REFERENCE

Use the provided Approve page screenshot as the visual reference.

The Create Payroll page should match its:

- Font style
- Font sizing and weight hierarchy
- Page density
- Spacing
- Alignment
- Input styling
- Button styling
- Border treatment
- Table styling
- Status badge styling
- Background color
- Overall enterprise banking/SaaS appearance

Do NOT copy Approve-page-specific content or functionality.

CREATE PAYROLL PAGE CONTENT

Keep the existing Create Payroll functionality and information.

The page should contain:

Top section

Use a compact page layout.

Show:

← Back to Payroll Batches

Then:

BATCH-1787801993513-553
with the existing Draft status.

Below the batch ID, display the existing metadata:

- Debit Account
- Currency
- Payment Date

Keep these aligned horizontally when space allows.

Do NOT place everything inside an oversized card.

Payee section

Create a clean section:

Payee Details (1)

Place:

+ Add Payee
Reset Rows

on the same horizontal line where possible.

Use the existing shared Button components.

The buttons should have the same visual language as the buttons on the Approve page:

- clean
- subtle border
- moderate height
- consistent radius
- professional blue accent
- no excessive rounded/pill styling

Payee table

Make the table visually similar to the table on the Approve page.

Keep the existing columns:

- Employee ID
- Name
- Account Number
- IFSC/SWIFT
- Amount
- Actions

Use:

- clean white rows
- subtle horizontal separators
- compact but comfortable row height
- clear column headers
- consistent font sizing
- proper column alignment
- sufficient but not excessive padding

The table header should use the application's existing blue design from the shared styles/variables.

Do NOT introduce a new gradient or unrelated color.

Keep the editable fields exactly as they currently function.

Amount

Keep the existing amount field and behavior.

Display the currency correctly, for example:

₹0.00

Keep the amount aligned appropriately with the other table values.

Remove action

Keep the existing Remove functionality.

Make the Remove action visually consistent with the Approve page's action buttons.

Do not change its handler or behavior.

Pagination

Keep the existing pagination functionality.

Use the same visual density and style as the Approve page.

Show:

Showing 1–1 of 1

on the left.

Pagination controls should be aligned to the right.

Avoid creating a large separate pagination card.

Totals

Keep:

Total Payees: 1

and

Total Amount: ₹0.00

Place these in a compact summary area below the table.

Do not turn them into large dashboard statistic cards.

Bottom actions

Keep the existing:

Back to list
Save as Draft
Submit for Approval

Use the same button styling approach as the Approve page.

Recommended hierarchy:

- Back to list → secondary/outlined
- Save as Draft → secondary/outlined
- Submit for Approval → primary blue

Keep all existing click handlers and functionality unchanged.

SPACING — VERY IMPORTANT

The current Create Payroll page has too much empty space.

Make it closer to the Approve page screenshot's density.

Reduce:

- excessive outer margins
- excessive card padding
- large gaps between batch information and Payee Details
- unnecessary vertical whitespace
- unnecessary height of containers

But do NOT make everything cramped.

The page should have the same balanced spacing rhythm as the Approve page.

CONTAINER / CARD DESIGN

Do NOT put the entire Create Payroll page inside one huge oversized card.

Instead:

- Use the normal page background.
- Use subtle containers only where useful.
- Keep the main content width aligned with the application's existing page layout.
- Keep the table visually prominent.
- Avoid huge empty areas.

The final page should feel compact and professional.

TYPOGRAPHY

Match the font appearance and hierarchy of the Approve page screenshot.

Use the existing application's typography variables/styles.

Do NOT introduce a new font library.

Use existing:

- font family
- font weights
- font sizes
- line heights

from the application's existing styling system.

The page title/batch ID should be prominent, section headings should be clear, and table text should remain compact and readable.

COLORS

Use the existing colors from "variables.css".

The visual direction should be:

- dark navy application header
- blue primary actions
- light/neutral page background
- white content areas
- subtle borders
- existing status colors

Do NOT change "variables.css".

Do NOT add global color variables.

RESPONSIVENESS

Maintain the existing responsive behavior.

Do not introduce horizontal scrolling for the entire page unnecessarily.

If the payee table requires horizontal scrolling at smaller widths, keep the scrolling limited to the table area.

CODE QUALITY

Before making changes:

1. Inspect the existing Create Payroll component.
2. Inspect the shared components already used by the project.
3. Inspect "variables.css" only to understand and reuse existing variables.
4. Reuse existing Button, Table, Input, Badge, Pagination, Card, and other shared components where applicable.
5. Do not modify those shared components.
6. Keep existing JSX/React logic wherever possible.
7. Change only the layout and page-specific styling required for the redesign.

FINAL CHECK

After implementation, verify:

- Approve page remains completely unchanged.
- Other pages remain unchanged.
- Shared components remain unchanged.
- "variables.css" remains unchanged.
- Global styles remain unchanged.
- API/business logic remains unchanged.
- Create Payroll functionality still works exactly as before.
- Only the Create Payroll page's appearance has changed.

Target result

The final Create Payroll page should look like it was designed by the same UI designer who created the Approve page in the reference screenshot — same font feel, same spacing rhythm, same table style, same button treatment, same blue/white enterprise banking aesthetic — while retaining all of the Create Payroll page's existing content and functionality.
