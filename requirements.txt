Yes. Since you need to give this to your company AI/code assistant, the prompt should be very explicit about what to change and what NOT to change. The key is to tell it to modify the shared dashboard layout/header/sidebar, not your payroll business logic.

Use this prompt as-is:

Redesign the existing Straight2Bank application dashboard UI so that it follows the same visual style, layout, spacing, typography, colors, navigation behavior, and profile interaction as the existing company Pulse dashboard shown in the reference.

IMPORTANT:

- This is a UI/UX redesign only.
- DO NOT change business logic.
- DO NOT change API calls.
- DO NOT change backend logic.
- DO NOT change existing routes.
- DO NOT remove existing functionality.
- DO NOT change payroll calculations, validations, CRUD operations, approval logic, or transaction logic.
- Reuse existing components and libraries wherever possible.
- Make the changes in the shared Dashboard/Layout/Header/Sidebar components so that all user stories automatically get the same design.
- Do not create a separate dashboard only for Payroll.
- The existing Manage Transactions, Payroll, Approve, Create, Manage, etc. pages must continue working exactly as before.

REFERENCE DESIGN:
The target design is the existing company Pulse dashboard.

==================================================

1. OVERALL APPLICATION STRUCTURE
   ==================================================

Create a dashboard layout with:

1. Fixed top header
2. Left navigation sidebar
3. Main content area
4. Light background behind the main content
5. White cards/content containers
6. User profile icon and dropdown in the top-right

Overall structure:

Header

Sidebar          Main Content
Page content
Cards
Tables
Forms

The header and sidebar must be shared by all pages.

==================================================
2. HEADER

Redesign the current Straight2Bank header to have a cleaner corporate banking appearance similar to the Pulse dashboard.

Header should contain:

LEFT:

- Hamburger/menu icon
- Standard Chartered/Straight2Bank logo
- Application name
- Navigation items such as:
  - Explore
  - Connect
  - Resources

RIGHT:

- Search icon
- Applications/grid icon
- Notification/bell icon
- User profile icon

The header should have a dark navy/blue background.

Use a consistent height around 60–68px.

Use proper horizontal spacing between items.

Do not overcrowd the header.

Header text should be small/medium sized and professional.

Use white/light text on the dark header.

==================================================
3. HEADER COLORS

Use a professional banking color palette.

Primary header:

- Dark navy blue

Example starting value:
#071B3A

Primary action blue:

- Blue similar to the company's existing banking blue

Example:
#0072CE

Main page background:

- Very light blue/gray

Example:
#F5F7FA

Cards:

- White

Text:

- Dark navy/gray

Secondary text:

- Muted gray

Borders:

- Very light gray

Do not use excessive bright green.

Green should mainly be used for:

- Success
- Approved status
- Positive indicators

Use subtle colors rather than very saturated colors.

If the existing project already contains company color variables/theme values, REUSE those values instead of introducing new ones.

==================================================
4. SIDEBAR

Redesign the sidebar to visually match the Pulse dashboard.

Sidebar should:

- Be positioned below the header.
- Have a clean light/white background OR the existing company's sidebar background depending on the existing design system.
- Have consistent icons.
- Have consistent spacing between menu items.
- Have clear active/selected state.
- Be vertically scrollable if menu items exceed the viewport.

Existing functionality must remain.

Current menu items such as:

Create
Manage
Approve
Upload
Balances

must remain available if they are existing application features.

Organize menu items logically.

Example:

PAYROLL

Create
Manage
Approve

OPERATIONS

Transactions
Upload
Balances

Do not invent routes that don't exist.

==================================================
5. SIDEBAR ACTIVE STATE

The currently selected page must have a clear active state.

Do NOT use the current very bright neon-green rectangular block.

Instead use a professional blue active state similar to the Pulse dashboard.

Example:

background:
#E6F3FF

text:
primary blue

icon:
primary blue

Optionally add a small blue indicator on the left.

The active menu item should have slightly rounded corners.

Example visual:

[ icon ]  Manage
^ active blue background

Inactive items should have:

- Dark gray text
- Muted icon
- White/light background

Hover state should use a very subtle light-blue/gray background.

==================================================
6. SIDEBAR ICONS

Use consistent professional icons.

Do not mix different icon styles.

If the project already uses an icon library such as:

- Lucide
- React Icons
- Material Icons

reuse the existing library.

Icons should be approximately 18–20px.

Text and icon should be vertically centered.

Keep consistent spacing between icon and text.

==================================================
7. PROFILE ICON

Add a profile icon in the top-right corner of the header similar to the Pulse dashboard.

The profile icon should be circular.

It can contain:

- User silhouette
  OR
- User avatar if one already exists.

Use a subtle circular background.

When clicked, open a profile dropdown.

==================================================
8. PROFILE DROPDOWN

The profile dropdown should visually resemble the company Pulse profile menu.

Example structure:

---

| 👤  User Name                |
|     User Role                |
|                              |
|       Edit Profile           |
|                              |
| Terms & Conditions            |
| Privacy Notice               |
| Terms of Use                 |
|                              |
| Sign Out                     |

The dropdown should:

- Be positioned below the profile icon.
- Have white background.
- Have subtle border.
- Have subtle shadow.
- Have rounded corners.
- Have proper padding.
- Have clear separation between profile information and actions.

Profile section:

User name
User role

For the current application, use the logged-in user's existing data.

Do NOT hardcode a user if the application already has logged-in user information available.

==================================================
9. PROFILE DROPDOWN COLORS

Profile dropdown:

background:
#FFFFFF

text:
dark navy/gray

secondary text:
muted gray

hover:
very light blue/gray

primary profile action:
light blue background

Sign Out:
subtle danger styling if appropriate.

Do not make the dropdown bright or colorful.

==================================================
10. MAIN PAGE BACKGROUND

The current application uses too much plain white space.

Change the main dashboard content background to a very light gray/blue.

Suggested:

#F5F7FA

The main page should occupy the remaining viewport after the header and sidebar.

Example:

.header
height: 64px

.main-layout
display: flex

.sidebar
width: approximately 220–250px

.main-content
flex: 1
background: #F5F7FA

==================================================
11. PAGE CONTENT CONTAINERS

Use white cards for important content.

Cards should have:

background:
white

border:
1px solid light gray

border-radius:
8–12px

subtle shadow if appropriate.

Do NOT use heavy shadows.

Do NOT make everything extremely rounded.

The UI should look like an enterprise banking application, not a consumer social-media application.

==================================================
12. TYPOGRAPHY

Make typography consistent throughout the application.

Use the existing company/project font if it is already configured.

DO NOT randomly introduce a new font.

If the project already has a typography/theme configuration, reuse it.

Recommended hierarchy:

Page title:
24–28px
medium/semibold

Section heading:
18–20px
medium/semibold

Normal text:
14–16px

Secondary text:
12–14px

Table text:
13–14px

Sidebar text:
14–15px

Header navigation:
14–15px

Button text:
14px

Do not make every text element bold.

Use font-weight consistently.

==================================================
13. PAGE TITLE

For pages such as:

Payroll Batches
Manage Transactions
Approvals

use a clean page title at the top.

Example:

Payroll Batches

Create, manage and track your payroll batches.

The title should be dark navy.

Subtitle should be muted gray.

Maintain consistent spacing below the title.

==================================================
14. PAYROLL DASHBOARD

Do not change the existing Payroll functionality.

Improve only its presentation.

The Payroll page should have:

Page title:

Payroll Batches

Subtitle:

Create, manage and track your payroll batches.

Then summary cards:

Total Batches
Drafts
Pending Approval
Approved
Total Payees
Total Amount

Cards should be white with subtle borders.

Numbers should be larger than labels.

Example:

TOTAL BATCHES
12

DRAFT BATCHES
3

PENDING APPROVAL
2

Use icons only if the project already has an icon library.

==================================================
15. QUICK ACTIONS

Add a clean Quick Actions section if it fits the existing Payroll page.

Examples:

Create New Batch
View Drafts
Pending Approvals
Payment History

The primary action should be:

+ Create New Batch

Use the company's primary blue.

Do not add functionality that does not exist.

If an action does not have an existing route/functionality, do not implement it.

==================================================
16. SEARCH AND FILTERS

Make search and filters look consistent.

Use:

Search batches...

Status
Payment Date
Currency

Inputs should have:

- White background
- Light border
- Consistent height
- Small border radius
- Proper padding
- Clear labels

Do not use oversized inputs.

==================================================
17. TABLE DESIGN

Keep the existing Payroll table functionality.

Improve only its styling.

Use:

White table container
Light borders
Clear header
Proper row spacing
Consistent typography

Table header:

- Slightly darker/muted text
- Medium font weight
- Light background

Rows:

- White background
- Subtle bottom border
- Hover state

Avoid excessive grid lines.

Amounts should be consistently aligned.

Dates should be consistently aligned.

==================================================
18. STATUS BADGES

Keep status badges but make them professional.

Draft:
neutral gray

Pending:
subtle amber

Approved:
subtle green

Rejected:
subtle red

Use small rounded badges.

Example:

● Pending

Do not use highly saturated colors.

==================================================
19. BUTTONS

Create a consistent button hierarchy.

Primary:

Create New Batch
Approve
Submit

Use primary blue.

Secondary:

View
Edit
Cancel

Use white background with border.

Danger:

Delete
Reject

Use subtle red styling.

All buttons should have:

- Consistent height
- Consistent padding
- Same border radius
- Same font
- Same font size

==================================================
20. MODALS

Do NOT use browser alert() or confirm().

Use the existing Modal component.

For example:

Delete Draft Batch?

Are you sure you want to delete this draft batch?
This action cannot be undone.

Cancel     Delete

Use Toast for successful operations.

Examples:

Batch created successfully.

Batch submitted successfully.

Draft deleted successfully.

==================================================
21. RESPONSIVE DESIGN

The dashboard should work correctly on:

- Laptop
- Desktop
- Tablet

Sidebar should not overlap content.

Header should remain usable.

Tables should handle smaller widths appropriately.

==================================================
22. IMPORTANT SHARED COMPONENT REQUIREMENT

Before creating anything new, inspect the project for existing:

Header
Navbar
Sidebar
DashboardLayout
Layout
Modal
Toast
Button
Icon
Theme
CSS variables
Tailwind configuration

Reuse these components.

If Header and Sidebar are already shared by multiple pages, modify the shared components rather than duplicating them.

This is especially important because multiple user stories are being developed by different team members.

For example:

DashboardLayout
├── Header
├── Sidebar
└── Page Content

Then:

Manage Transactions
Payroll Batches
Approvals
Create Payroll

should all use the same DashboardLayout.

==================================================
23. DO NOT BREAK EXISTING USER STORIES

The application currently contains multiple user stories implemented by different developers.

Do not modify routes or page functionality unnecessarily.

If a developer already has:

Manage Transactions

and another developer has:

Payroll Batches

both must continue working.

Only the common visual shell should be standardized.

==================================================
24. FINAL VISUAL GOAL

The final application should feel like one professional corporate banking application.

The visual characteristics should be:

- Dark navy/blue header
- Clean sidebar
- Blue active navigation state
- Light gray/blue page background
- White content cards
- Consistent typography
- Professional profile icon
- Profile dropdown
- Clean icons
- Subtle borders
- Subtle shadows
- Consistent spacing
- Professional buttons
- Clean status badges
- Clear page hierarchy

The design should take inspiration from the existing company Pulse dashboard but should NOT copy proprietary source code.

Use the existing application's components, data, routes and business logic.

==================================================
25. IMPLEMENTATION ORDER

Because this is an existing application, implement in this order:

1. Inspect existing Dashboard/Layout component.
2. Identify shared Header.
3. Identify shared Sidebar.
4. Identify existing theme/CSS/Tailwind configuration.
5. Update Header.
6. Update Sidebar.
7. Add profile icon/dropdown.
8. Update application background.
9. Update typography.
10. Update common buttons/cards.
11. Update Payroll page styling.
12. Verify Manage Transactions still works.
13. Verify Create Payroll still works.
14. Verify Approve still works.
15. Verify routes have not changed.
16. Verify no backend/API/business logic has changed.

Do not spend time rewriting components that already work.

The priority is to make the application visually consistent with the company dashboard while preserving all existing functionality.
