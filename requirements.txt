Use this prompt
Change the three-dot Actions dropdown positioning.
Currently, when the ⋮ button is clicked, the dropdown appears above or below the button, which looks awkward in this table.
I want the dropdown to open horizontally to the LEFT of the three-dot button, aligned vertically with the button.
Desired layout:
View   ⋮   [ View Payee Details / Print ]
More specifically:
Keep the View button and ⋮ button exactly as they are.
When ⋮ is clicked, position the dropdown to the left of the three-dot button.
Vertically center the dropdown relative to the ⋮ button.
Maintain a small gap of approximately 8px between the button and dropdown.
Give the dropdown enough width (around 190–210px) so View Payee Details is displayed completely.
Do not overlap the View button.
Do not push or resize the table row.
Do not change the sidebar, table, colors, fonts, or other components.
Make sure the dropdown remains inside the viewport at different screen sizes.
If there isn't enough space to the left, then intelligently fall back to opening above/below.
Clicking outside should close the dropdown.
The preferred/default placement is LEFT of the three-dot button, not above or below. Make the positioning clean and professional like a modern banking/enterprise application.
