Polish the existing Create Payroll page based on its current implementation.

The current layout and overall spacing are acceptable. Do not redesign the page from scratch. Make only the UI refinements needed to improve clarity, alignment, readability, and button styling.

STRICT SCOPE

- Modify ONLY the Create Payroll page.
- Do NOT modify any other page.
- Do NOT modify the Approve page.
- Do NOT modify shared components.
- Do NOT modify "variables.css".
- Do NOT modify global CSS/global styles.
- Reuse existing shared components.
- Reuse existing variables from "variables.css".
- Do not introduce new global variables.
- Do not change API calls, state, validation, handlers, routing, pagination, or business logic.
- Do not change any existing functionality.

1. Overall page

Keep the current layout and current compact spacing.

Make the page look:

- clean
- professional
- balanced
- easy to scan
- consistent with the existing Approve page

Do not add unnecessary cards, statistics, illustrations, or extra sections.

Do not increase the overall height of the page.

2. Batch header

Keep:

← Back to Payroll Batches

BATCH-1787801993513-553

Draft

And:

- Debit Account
- Currency
- Payment Date

Improve the alignment and readability.

The batch ID should be slightly more prominent.

The metadata should have clear spacing between each item instead of looking like one continuous line.

Keep the Draft status neatly aligned to the right.

Do not create excessive whitespace.

3. Payee Details section

Keep:

Payee Details (1)

Place:

+ Add Payee   Reset Rows

on the same row.

Improve these buttons

The current Add Payee and Reset Rows buttons look slightly oversized and visually inconsistent.

Make them:

- smaller
- equal in height
- compact
- clearly readable
- properly aligned
- consistent with the existing application button style

Use:

Add Payee

- blue outline/primary-accent appearance
- plus icon if the existing shared icon is available

Reset Rows

- neutral/secondary outline appearance
- reset icon only if an existing shared icon is available

Do not make them heavily rounded or oversized.

Keep a small, consistent gap between the buttons.

Do not change their functionality.

4. Payee table

Keep the existing table and columns:

- Employee ID
- Name
- Account Number
- IFSC/SWIFT
- Amount
- Actions

Improve the table so that the content is easier to read.

Alignment

This is very important.

Make sure:

- Employee ID is aligned correctly.
- Name is aligned correctly.
- Account Number is aligned correctly.
- IFSC/SWIFT is aligned correctly.
- Amount is aligned consistently.
- Actions is aligned exactly under the Actions header.

The table should not look shifted or uneven.

5. Table inputs

Keep the existing editable inputs.

Make the inputs:

- consistent in height
- consistent in padding
- properly aligned within their columns
- not unnecessarily wide
- not unnecessarily tall

Use the existing shared Input component/style if available.

Do not change validation or input behavior.

6. Remove button

The current Remove button is too faint/small and does not look properly aligned with the Actions column.

Improve it so it is:

- clearly visible
- compact
- properly aligned under Actions
- easy to understand
- visually consistent with the application

Use a subtle destructive/red treatment if an existing shared destructive button style is available.

Do not make it large.

Do not change its functionality.

7. Table background

Give the table a subtle visual separation from the page.

Use the existing design variables.

For example:

- very light neutral/blue table background
- subtle border
- clean blue table header
- white/light table rows

Do not introduce new colors.

Do not use a strong gradient.

8. Pagination

Keep the existing pagination functionality.

Align it cleanly:

Showing 1–1 of 1 on the left.

Pagination controls on the right:

First | Prev | 1 | Next | Last

Make all pagination buttons compact and equal in height.

Do not create a separate large card for pagination.

9. Totals

Keep:

Total Payees: 1

Total Amount: ₹0.00

Give this section a subtle light background and enough separation from pagination.

Keep it compact.

Do not turn it into large dashboard cards.

Align the two values neatly.

10. Bottom action buttons

Keep:

Back to list
Save as Draft
Submit for Approval

Improve their styling and alignment.

All three buttons should:

- have the same height
- have consistent padding
- have consistent font size
- have consistent border radius
- have consistent spacing

Use this hierarchy:

Back to list

- secondary
- outlined/neutral

Save as Draft

- secondary
- blue outlined or neutral style
- DO NOT use bright green

Submit for Approval

- primary
- solid application blue

Keep them compact.

Do not make them unnecessarily wide.

Align all three buttons neatly on the right.

Do not change their handlers or functionality.

11. Typography

Match the existing Approve page typography as closely as possible.

Use the existing application's font and typography variables.

Improve:

- heading weight
- table readability
- button text
- metadata readability

Do not add a new font.

Do not change global typography.

12. Spacing

The current spacing is mostly good, so make only small refinements.

Specifically:

- reduce any remaining unnecessary gap between batch information and Payee Details
- keep Payee Details close to the table
- keep pagination close to the table
- keep totals close to pagination
- keep bottom actions close to totals

Do not make the page cramped.

Do not increase the page height.

FINAL REQUIREMENT

This is a UI polish task, not a functional or architectural change.

Use the existing shared components and "variables.css".

Do not modify "variables.css", shared components, global styles, or any other file.

Only improve the visual presentation of the existing Create Payroll page.

Preserve all existing functionality exactly as it is.
