Fix the three-dot dropdown positioning in the Actions column.
Required behavior:
The dropdown should prefer to open on the RIGHT side of the three-dot (⋮) button.
It must remain completely inside the browser viewport.
If opening fully to the right would make any part of the dropdown go outside the viewport, automatically shift the dropdown left only by the amount necessary to keep it visible.
Do NOT always position the dropdown on the left.
Do NOT let it overflow outside the browser window.
Do NOT create horizontal scrolling.
Keep the dropdown close to the ⋮ button with an approximately 6–8px gap.
Give the dropdown a fixed width around 180–200px.
Keep View Details and Print fully visible.
The dropdown should not overlap the View button.
If there is insufficient vertical space below the button, flip it vertically and open above the button.
The positioning should dynamically respond when the sidebar is expanded/collapsed and when the browser window is resized.
Implementation guidance: Use the three-dot button as the positioning anchor and calculate/check the available viewport space before positioning the dropdown. Do not rely on hardcoded left, right, top, or negative offsets that only work at one screen size.
Also fix the Actions column so the View and ⋮ buttons do not shrink or produce unwanted ... when the sidebar is expanded.
Do not change the existing colors, fonts, table design, sidebar functionality, backend, or unrelated components. Only fix the Actions column responsiveness and dropdown positioning.
