Fix the inconsistent spacing between the shared Sidebar and the main page content.

Currently, when navigating between Create, Manage, Approve, Upload and Balances, the main content starts at different horizontal positions. This makes the dashboard layout inconsistent.

IMPORTANT:
Do not modify the business logic or functionality of any page.

Use the shared dashboard layout to provide consistent positioning.

Requirements:

1. Sidebar width must remain consistent across all dashboard pages.

2. Main content should always begin at the same horizontal position after the sidebar.

3. Use one common gap/padding between:
   Sidebar → Main Content

4. Do NOT add different margin-left values individually to Create, Manage, Approve, Upload or Balances.

5. The shared layout should control the structure, for example:

   [ Sidebar ][ consistent gap ][ Main Content ]

6. Give the main content consistent padding, approximately:
   padding: 32px 40px;

   Adjust slightly if required to match the existing design.

7. Remove page-specific margin-left/padding-left values that are causing inconsistent alignment, ONLY where safe.

8. Main content should use the available remaining width instead of fixed widths that cause horizontal overflow.

9. Avoid unnecessary horizontal scrolling.

10. When navigating:
    Create → Manage → Approve → Upload → Balances

    the left edge of the main content should remain visually aligned.

11. Keep the existing Header and Sidebar appearance and functionality unchanged.

12. Do not change APIs, routing, state management, validation or business logic.

13. Prefer fixing this through the existing shared Dashboard/Layout component rather than repeating CSS in every page.

IMPORTANT:
Do not redesign unrelated components. Only normalize the dashboard layout spacing/alignment.
